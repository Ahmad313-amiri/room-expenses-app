import 'package:flutter/material.dart';

class OwedCard extends StatelessWidget {
  final Icon icon;
  final String owedText;
  final String amount;

   OwedCard({super.key,required this.icon, required this.owedText, required this.amount});

  @override
  Widget build(BuildContext context) {
    return Card(

      child: Column(
        children: [


        ],
      ),


    );
  }
}

